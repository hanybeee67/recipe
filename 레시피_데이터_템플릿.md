# 레시피 데이터 템플릿 (스키마 정의서)

Everest Restaurant Group — 레시피북 앱의 **단일 진실 공급원(Single Source of Truth)** 스키마.

원본 `data/에베레스트_레시피_v3.xlsx` 의 각 시트 1개 = `recipes/*.md` 파일 1개로 1:1 대응한다.
앱은 이 마크다운을 직접 읽어(빌드 시 파싱) 데이터를 만들며, 별도의 DB를 두지 않는다.

---

## 1. 파일 규칙

| 항목 | 규칙 |
|---|---|
| 위치 | `recipes/<id>.md` |
| 파일명(=`id`) | 영문명 기준 kebab-case 슬러그. 예: `Chicken Makhani` → `chicken-makhani.md` |
| 인코딩 | UTF-8 (BOM 없음) |
| 개행 | LF |
| 구성 | YAML Front Matter + 마크다운 본문 |

---

## 2. Front Matter 스키마

```yaml
---
id: chicken-makhani          # string, 필수, 고유. 파일명과 반드시 일치
name: 치킨머커니              # string, 필수. 한글 메뉴명 (원본 B2)
nameEn: Chicken Makhani      # string, 필수. 영문명 (원본 B3 괄호 안)
category: 닭고기 카레          # string, 필수. 원본의 세부 분류 (원본 B3 괄호 앞)
group: 커리                   # string, 필수. 앱 상단 탭용 대분류 (아래 3항 매핑표)
order: 0                     # number, 필수, 고유. 메뉴판 나열 순서 (4항)
serving: 1인분 기준            # string, 필수. 계량 기준 (원본 G2)
cookTime: 약 5~8분            # string, 필수. 표기용 조리시간 (원본 B4)
cookTimeMin: 5               # number, 필수. 정렬/필터용 최소 분
cookTimeMax: 8               # number, 필수. 정렬/필터용 최대 분
image: images/recipes/chicken-makhani.png   # string|null. 원본 시트 임베드 사진 경로
imageNote: null              # string|null. 원본 사진이 아닐 때 그 출처 설명
ingredientCount: 8           # number, 필수. 재료 개수 (파생값, 검증용)
stepCount: 5                 # number, 필수. 조리 단계 수 (파생값, 검증용)
tags:                        # string[], 필수(빈 배열 허용). 검색/필터 보조 키워드
  - 닭고기
  - 크림
  - 마일드
---
```

### 필드 상세

- **`id`** — URL 라우팅 키(`#/recipe/chicken-makhani`). 영문명이 없으면 한글명 로마자 슬러그로 대체.
- **`group`** — 필터 UI의 1차 축. 값은 아래 3항의 9종으로 **고정**한다.
- **`cookTimeMin/Max`** — `약 5~8분` → `5`, `8`. 단일값(`약 5분`)이면 min=max.
- **`image`** — 원본 시트에 사진이 없으면 `null`. 앱은 이 경우 "사진 없음" 플레이스홀더를 렌더한다.
- **`imageNote`** — 다른 메뉴의 사진을 빌려온 경우 그 사실을 남긴다. 상세 화면과 인쇄 카드에
  작은 각주로 표시된다. 원본 사진이면 `null`.
- **`order`** — 메뉴판 나열 순서. `tools/convert_xlsx.py` 가 자동 계산하며 전체에서 유일해야 한다.
- **`tags`** — 주재료·조리법·맛 프로필에서 자동 도출. 검색 시 `name`/`nameEn`/`재료명`과 함께 매칭된다.

---

## 3. `group` 대분류 매핑 (고정 9종)

표의 위에서 아래 순서가 곧 메뉴판 순서다.

| 순서 | `group` | 포함되는 `category` |
|---:|---|---|
| 1 | `커리` | 닭고기 카레, 닭고기 요리, 양고기 카레, 채식 카레, 베지 카레, 콩 카레, 계란 카레, 새우 카레, 해산물 카레 |
| 2 | `탄두리` | 탄두리 요리 |
| 3 | `스낵` | 스낵, 사이드 메뉴 |
| 4 | `빵류` | 빵류 |
| 5 | `밥·면` | 밥류, 볶음면, 채식 볶음면, 국수 수프 |
| 6 | `수프·샐러드` | 수프, 샐러드 |
| 7 | `세트` | 세트 메뉴 |
| 8 | `디저트` | 디저트, 디저트/사이드, 사이드/디저트 |
| 9 | `음료` | 음료 |

---

## 4. 나열 순서 (`order`)

목록·PDF 목차·인쇄 순서가 모두 `order` 오름차순을 따른다. 값은
`tools/convert_xlsx.py` 의 세 목록으로 계산한다.

```
order = GROUP_ORDER 위치 × 1,000,000
      + CATEGORY_ORDER 위치 × 10,000
      + ITEM_ORDER 위치
```

- **`GROUP_ORDER`** — 위 3항의 9종 순서
- **`CATEGORY_ORDER`** — 대분류 안에서의 세부 카테고리 순서
  (커리는 치킨 → 머턴 → 야채 → 계란 → 해산물)
- **`ITEM_ORDER`** — 카테고리 안에서의 개별 메뉴 순서
  (빵류는 플레인 난 → 버터 난 → 갈릭 난 → 치즈 난 → 허니 난 → 굴자빵 → 알루 파라타 → 탄두리 로티)

세 목록 중 하나에 없는 값은 맨 뒤로 밀리고 변환 시 경고가 뜬다.
메뉴 순서를 바꾸려면 **`recipes/*.md` 가 아니라 `tools/convert_xlsx.py` 의 목록을 고치고
다시 변환한다.**

---

## 5. 본문 스키마

본문은 **`## 재료` → `## 조리 방법` → `## 가니쉬`** 3개 섹션을 이 순서로 갖는다.
파서는 H2 제목 텍스트로 섹션을 식별하므로 제목 문구를 변경하면 안 된다.

### 5-1. `## 재료`

GFM 표. 컬럼 4개 고정, 헤더 문구 고정.

```markdown
## 재료

| # | 재료명 | 수량 | 비고 |
|---:|---|---|---|
| 1 | 닭가슴살 (정육) | 100g | 탄두리 마리네이드 후 구운 것 |
| 2 | 커리 그레이비 소스 | 200g | 베이스 소스 |
| 3 | 꽃소금 | 5g | |
```

- `비고`가 없으면 빈 셀로 둔다(열은 생략하지 않는다).
- `수량`은 원본 표기를 그대로 유지한다(`100g`, `300ml`, `1개` 등).

### 5-2. `## 조리 방법`

번호 목록(ordered list). 1단계 = 1항목. 원본의 단계 번호를 그대로 승계한다.

```markdown
## 조리 방법

1. 준비된 삶은 닭가슴살을 큐브형태로 자른다.
2. 팬에 커리 그레이비 소스를 넣어 중불로 가열하고 설탕을 넣어 단맛을 조절한다.
3. 구운 닭을 한 입 크기로 잘라 소스에 넣고 약불로 3-4분 조린다.
```

### 5-3. `## 가니쉬`

자유 문단 1개. 원본에 없으면 섹션 자체를 생략한다.

```markdown
## 가니쉬

완성된 커리 위에 생크림. 체리 1개와 생강, 고수잎으로 마무리.
```

---

## 6. 전체 예시

```markdown
---
id: chicken-makhani
name: 치킨머커니
nameEn: Chicken Makhani
category: 닭고기 카레
group: 커리
order: 3
serving: 1인분 기준
cookTime: 약 5~8분
cookTimeMin: 5
cookTimeMax: 8
image: images/recipes/chicken-makhani.png
imageNote: null
ingredientCount: 8
stepCount: 5
tags:
  - 닭고기
  - 크림
---

## 재료

| # | 재료명 | 수량 | 비고 |
|---:|---|---|---|
| 1 | 닭가슴살 (정육) | 100g | 탄두리 마리네이드 후 구운 것 |
| 2 | 커리 그레이비 소스 | 200g | 베이스 소스 |

## 조리 방법

1. 준비된 삶은 닭가슴살을 큐브형태로 자른다.
2. 팬에 커리 그레이비 소스를 넣어 중불로 가열한다.

## 가니쉬

완성된 커리 위에 생크림. 체리 1개와 고수잎으로 마무리.
```

---

## 7. 검증 규칙

빌드 시 `scripts/build-recipes.mjs` 가 아래를 검사하고, 위반 시 **빌드를 실패**시킨다.

1. `id` 가 파일명(확장자 제외)과 일치하는가
2. `id` 가 전체에서 유일한가
3. 필수 필드가 모두 존재하고 타입이 맞는가
4. `group` 이 고정 9종 중 하나인가
5. `ingredientCount` = 재료 표의 실제 행 수, `stepCount` = 조리 단계 실제 개수
6. `image` 가 `null` 이 아니면 `public/` 하위에 해당 파일이 실제로 존재하는가
7. 재료 표 헤더가 `# / 재료명 / 수량 / 비고` 인가
8. `order` 가 다른 레시피와 중복되지 않는가

---

## 8. 원본 xlsx 셀 매핑

| 원본 셀 | 대상 필드 |
|---|---|
| `B2` | `name` |
| `G2` | `serving` |
| `B3` | `category` + `nameEn` (`📂 {category} ({nameEn})`) |
| `B4` | `cookTime`, `cookTimeMin`, `cookTimeMax` |
| `D7:G*` | `## 재료` 표 (번호/재료명/수량/비고) |
| `👨‍🍳 조리 방법` 이후 B열 | `## 조리 방법` |
| `🌿 가니쉬` 이후 B열 | `## 가니쉬` |
| 시트 임베드 이미지 | `image` |

원본 87시트 중 4개에는 그림이 없다.

| 메뉴 | 처리 |
|---|---|
| 허니 난 | 플레인 난 사진을 빌려 쓰고 `imageNote` 로 표시 (원본도 치킨/머턴 커리에 같은 사진을 쓴다) |
| A / B / C set | 사진 없이 비워 둔다 |

변환은 `tools/convert_xlsx.py` 로 재현 가능하다.

```bash
python3 tools/convert_xlsx.py
```

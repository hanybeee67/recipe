# 레시피북 앱 기획서

**Everest Restaurant Group — 주방용 레시피북 웹앱**

---

## 1. 배경 및 목표

### 1-1. 문제

현재 전 매장의 레시피는 `에베레스트_레시피_v3.xlsx` 단일 엑셀 파일(시트 87개)로 관리된다.

- 주방에서 태블릿/폰으로 엑셀을 열면 셀이 잘리고 확대·축소를 반복해야 한다
- "닭고기 없는 커리", "10분 안에 되는 것" 같은 **검색이 불가능**하다
- 신규 직원 교육 시 시트를 하나씩 인쇄해야 하고, 인쇄물이 곧 구버전이 된다
- 사진이 시트에 임베드되어 있어 플레이팅 참고용으로 꺼내 쓰기 어렵다

### 1-2. 목표

> **주방 현장에서 폰 한 대로 87개 레시피를 3초 안에 찾고, 그대로 보고 조리하고, 필요하면 인쇄한다.**

| 지표 | 목표 |
|---|---|
| 레시피 탐색 시간 | 3초 이내 (검색어 2~3글자) |
| 오프라인 동작 | 최초 로딩 후 전체 데이터 캐시 |
| 지원 화면 | 360px(폰) ~ 1920px(주방 모니터) |
| 데이터 갱신 | md 파일 수정 → 커밋 → 자동 반영 |

### 1-3. 사용자

| 사용자 | 상황 | 핵심 니즈 |
|---|---|---|
| **조리사** | 화구 앞, 젖은 손, 폰 세워둠 | 큰 글씨, 재료/순서만, 스크롤 최소 |
| **신입 교육생** | 사무실, 태블릿 | 사진 + 상세 설명, 카테고리별 훑어보기 |
| **점장** | PC | 검색·비교, 신입 교육용 인쇄물 출력 |

---

## 2. 기능 우선순위

개발은 **아래 순서대로 진행한다.** 앞 단계가 완결되기 전에 뒷 단계로 넘어가지 않는다.

### 🥇 P1 — 상세보기 (Detail View)

> 레시피북의 본체. 이것만 있어도 앱은 최소한의 가치를 한다.

**요구사항**

1. **목록 → 상세** 이동
   - 목록: 사진 카드 그리드. 카드에 `한글명 / 영문명 / 카테고리 / 조리시간 / 재료 수`
   - 상세: 해시 라우팅 `#/recipe/<id>` — 새로고침·뒤로가기·링크 공유가 모두 동작해야 함
2. **상세 화면 구성** (위 → 아래)
   - 히어로: 요리 사진, 한글명, 영문명, 카테고리 배지, 조리시간, 1인분 기준 표기
   - 재료 표: 번호 / 재료명 / 수량 / 비고 — 모바일에서는 카드형으로 리플로우
   - 조리 방법: 번호가 크게 보이는 단계 리스트. **각 단계를 탭하면 완료 체크** (조리 중 위치 기억)
   - 가니쉬: 별도 강조 블록
3. **분량 배율기 (Serving Scaler)**
   - 원본은 전부 `1인분 기준`. `×1 / ×2 / ×3 / ×4` 토글로 재료 수량을 자동 환산
   - `100g` → `200g`, `300ml` → `600ml` 처럼 숫자+단위만 스케일. 비수치 표기는 원문 유지
4. **이전/다음 레시피 이동** — 현재 필터 결과 내 순서 기준
5. 접근성: 키보드 탐색 가능, 대비 4.5:1 이상, 조리 단계 글자 최소 16px

**완료 기준**: 87개 전 레시피가 누락·깨짐 없이 상세 화면에 렌더된다.

---

### 🥈 P2 — 검색 / 필터 (Search & Filter)

> 87개는 스크롤로 찾기엔 많다. 탐색 시간을 3초로 줄이는 단계.

**요구사항**

1. **통합 검색** — 입력 즉시 반영(디바운스 150ms)
   - 매칭 대상: 한글명, 영문명, 카테고리, **재료명**, 태그
   - "감자"로 검색하면 알루 고비·알루 파라타·알루덤·프렌치 프라이가 나와야 한다
   - 초성 검색 지원: `ㅊㅋ` → 치킨 커리, 치킨 마살라 …
   - 매칭된 부분 하이라이트
2. **대분류 필터** — `전체 / 커리 / 탄두리 / 스낵 / 빵류 / 밥·면 / 수프·샐러드 / 세트 / 디저트 / 음료`
   - 각 탭에 개수 배지
3. **세부 필터** (다중 선택)
   - 카테고리 (치킨 커리, 양고기 카레 …)
   - 조리시간: `5분 이내 / 10분 이내 / 전체`
   - 재료 제외: 특정 재료를 **포함하지 않는** 레시피만 (알레르기·비건 대응)
4. **정렬** — 이름순 / 조리시간 짧은순 / 재료 적은순
5. **필터 상태를 URL에 반영** (`#/?q=치킨&group=커리`) — 공유·새로고침 유지
6. 결과 0건 시 안내 + 필터 초기화 버튼

**완료 기준**: 임의의 재료명 검색으로 해당 재료를 쓰는 모든 레시피가 정확히 나온다.

---

### 🥉 P3 — PDF 내보내기 (PDF Export)

> 교육용 인쇄물과 매장 비치용 바인더를 앱에서 바로 뽑는다.

**요구사항**

1. **단일 레시피 내보내기** — 상세 화면에서 A4 1장 레시피 카드
2. **선택 내보내기** — 목록에서 체크박스로 여러 개 선택 → 한 문서로
3. **현재 필터 결과 전체 내보내기** — 예: "커리 전체 레시피북"
4. **표지 + 목차 자동 생성** (2건 이상 선택 시)
5. **인쇄 레이아웃 요구**
   - A4 세로, 레시피당 페이지 분리(`page-break-after`)
   - 재료 표는 페이지 중간에서 잘리지 않게(`break-inside: avoid`)
   - 한글이 이미지가 아닌 **선택 가능한 텍스트**로 남을 것
   - 헤더/푸터에 메뉴명·페이지 번호·`Everest Restaurant Group`
   - UI 요소(검색창, 버튼, 네비)는 인쇄에서 제외
6. 구현은 브라우저 인쇄 파이프라인(`@media print` + `window.print()`)을 사용한다.
   - 이유: 한글 폰트 임베딩이 확실하고, 텍스트가 벡터로 남아 인쇄 품질이 좋으며, 추가 런타임 의존성이 없다
   - 사용자는 인쇄 대화상자에서 `PDF로 저장`을 선택한다

**완료 기준**: 87개 전체를 한 번에 내보내도 레이아웃이 깨지지 않는다.

---

### 🅴 영어판 (추가 완료)

주방 인력 상당수가 한국어보다 영어가 편하다. 디자인과 정보 구조는 그대로 두고
언어만 바꾼다.

1. 헤더의 `한 / EN` 스위치. 선택은 기기에 저장하고 `?lang=en` 으로 공유 가능
2. UI 문구뿐 아니라 **재료명·계량 비고·조리 단계·가니쉬까지 전부 번역** (약 750개 문자열)
3. 검색 인덱스는 한/영을 함께 담아 어느 화면에서든 양쪽이 다 찾힌다
4. 번역 누락 시 빌드 실패 — 영어 화면에 한국어가 새어나오지 않는다
5. 인쇄(PDF)도 선택한 언어로 나온다

**완료 기준**: 영어 화면 어디에도 한국어가 남아 있지 않다 (메뉴명 부제는 의도적으로 병기).

### 향후 (범위 외)

- 즐겨찾기 / 최근 본 레시피
- 원가 계산 (재료 단가 연동)
- 레시피 편집 UI
- 네팔어 추가

---

## 3. 정보 구조

```
목록 (#/)
 ├─ 검색 · 필터 · 정렬 · 다중선택
 └─ 상세 (#/recipe/<id>)
     ├─ 히어로 (사진 / 명칭 / 메타)
     ├─ 재료 (배율기)
     ├─ 조리 방법 (단계 체크)
     ├─ 가니쉬
     └─ 이전 / 다음
인쇄 뷰 (#/print?ids=...)  ← P3
```

---

## 4. 데이터

- 스키마: **`레시피_데이터_템플릿.md`** 참조
- 원본: `data/에베레스트_레시피_v3.xlsx` (87 시트)
- 변환: `python3 tools/convert_xlsx.py` → `recipes/*.md` + `public/images/recipes/*.png`
- 빌드: `scripts/build-recipes.mjs` 가 `recipes/*.md` 를 검증 후 `src/data/recipes.json` 생성

레시피 수정은 **md 파일만** 고친다. 앱 코드에 데이터를 하드코딩하지 않는다.

---

## 5. 디자인 방향

**컨셉: "주방의 스테인리스 위에 놓인 향신료"** — 절제된 뉴트럴 배경 + 향신료 계열 강조색.

| 항목 | 값 |
|---|---|
| 강조색 | Saffron `#E07A2F` / Deep Paprika `#B03A1E` |
| 배경 | Light `#FDFCFA` (기본) / Dark `#141210` (수동) |
| 텍스트 | Light `#1A1613` / Dark `#F2EDE6` |
| 서체 | Pretendard → system-ui 폴백 |
| 라운드 | 카드 16px, 배지 999px |
| 그리드 | 360px: 1열 / 640px: 2열 / 1024px: 3열 / 1440px: 4열 |

- **라이트가 기본** — 카카오톡 등 인앱 브라우저는 시스템 다크를 따라가므로, 자동 추종은
  하지 않고 다크는 사용자가 직접 켰을 때만 적용한다
- 터치 타겟 최소 44×44px (젖은 손·장갑 대응)
- 애니메이션은 120~200ms, `prefers-reduced-motion` 존중

---

## 6. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 빌드 | Vite 7 | 빠른 개발 서버, 정적 산출물 |
| UI | React 19 + TypeScript | 상태(필터·선택·체크) 관리 명확 |
| 데이터 | 빌드 타임 md → JSON | 런타임 파싱·네트워크 요청 없음 |
| 스타일 | 순수 CSS (디자인 토큰 + BEM 계열 네이밍) | 런타임 의존성 0, 인쇄 스타일을 전역에서 한 번에 제어 |
| 라우팅 | 해시 라우팅 (자체 구현) | 정적 호스팅에서 404 없음 |
| PDF | `@media print` + `window.print()` | 한글 벡터 텍스트, 무의존성 |

런타임 의존성은 `react`, `react-dom` 둘뿐이다.
배포는 정적 파일 호스팅(GitHub Pages 등)을 가정하며, `vite.config.ts` 의 `base: "./"` 로
어느 하위 경로에 올려도 동작한다.

---

## 7. 개발 순서 체크리스트

- [x] 0. xlsx → `recipes/*.md` 87개 + 사진 83장 추출, 스키마 검증 통과
- [x] 1. **P1** 목록 그리드 → 상세 라우팅 → 재료/단계/가니쉬 → 배율기 → 단계 체크 → 이전/다음
- [x] 2. **P2** 통합 검색(재료·초성 포함) → 대분류 탭 → 세부 필터 → 정렬 → URL 동기화
- [x] 3. **P3** 인쇄 레이아웃 → 단일/선택/전체 내보내기 → 표지·목차
- [x] 4. 반응형 마감, 다크모드, 접근성, 빌드 검증
- [x] 5. **영어판** — 750여 개 문자열 번역, 언어 스위치, 라이트 기본 테마

### 검증 결과

| 항목 | 결과 |
|---|---|
| 레시피 변환 | 87/87 (사진 83장, 세트 3종·허니 난은 원본에 사진 없음) |
| 스키마 검증 | 87건 전부 통과 (`npm run data`) |
| 타입 검사 / 빌드 | 통과 (`npm run build`) |
| 재료 검색 | `감자` → 8건 (굴자빵·머라이 코프타·사모사·알루 고비·알루 파라타·알루 펄럭·알루덤·프렌치 프라이) |
| 초성 검색 | `ㅊㅋㅋㄹ` → 치킨 커리·치킨 코르마·A set. 어절 시작에서만 매칭해 `ㅊㅋ`가 "야**채 커**리"에 걸리지 않음 |
| PDF 87건 일괄 | 89쪽(표지 1 + 목차 1 + 레시피 87). **레시피 중 A4 한 장을 넘기는 것 0건** (최대 치킨 칠리 966px / 한도 1032px) |
| 콘솔 오류 | 없음 (목록·상세·검색·인쇄 전 경로) |
| 영어 번역 | 재료 136 · 비고 94 · 조리 단계 417 · 가니쉬 84 · 분류 24 · 태그 27 — 누락 0 |
| 영어 재료 검색 | `potato` → 8건 (한국어 `감자` 결과와 동일) |
| 교차 언어 검색 | 영어 화면에서 `감자`, 한국어 화면에서 `potato` 모두 8건 |
| 기본 테마 | 시스템 다크가 켜진 기기에서도 `data-theme="light"` 로 열림 |
